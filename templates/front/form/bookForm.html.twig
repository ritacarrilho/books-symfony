{% extends 'base.html.twig' %}

{% block title %}Books Form {% endblock %}


{% block body %}
    <div class="container mt-3">
        <h2>Books Formulary Page</h2>
        <div class="row">
            <div class="col-sm-3">
                    {{ include("front/template_part/_menu.html.twig", { pp: 'books', current: 0 }) }}
            </div>
            <div class="col-sm-9">
                {{ form_start(form) }}
                {{ form_rest(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
